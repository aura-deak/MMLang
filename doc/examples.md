# 演示程序

## 将混沌数据纸带所有的格子置零

```
#0
f x x > l
```

## 简单双程序纸带死循环演示

用于展示多程序纸带写法和s指令

```
#0
ns
#1
ns
```

## Hello World ASCII编码输出

每行代表一个字符的 ASCII 8 位编码写入。
`b` 截断数据纸带为输出，`!` 立即停机。

```
#0
(>x>>)*2(>)*2
(>x)*2(>)*3x(>)*2x>
(>x>x>)*2(>)*2
(>x>x>)*2(>)*2
(>x>x>)*2(x>)*2
(>)*2x(>>>)*2
(>x>)*3(x>)*2
(>x>x>)*2(x>)*2
(>x)*3(>)*3x(>)*2
(>x>x>)*2(>)*2
(>x)*2(>)*3x(>)*3
b
!
```